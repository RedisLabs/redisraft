* Code cleanup / reorg / memory free
    -> valgrind
* Networking, handle conn state and reconnects
    -> reconnect intervals
    -> hiredis related fixes
* INFO commands
    -> add stats about network connections, etc.
* Membership management, add/remove node
    -> add 'join' command to automate joining
    -> option to pre-configure nodes statically
* Cluster key
* Snapshotting/compaction
* Refactor Raft library with better types (e.g. log index > int)
* Command hooking for transparency
    -> currently a non-standard api extension
* Binary / side-band / datagram messaging
    -> datagram may not work well due to size
    -> consider different channel for performance
* Persistence
